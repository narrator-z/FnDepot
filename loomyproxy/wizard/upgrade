[
  {
    "stepTitle": "升级说明",
    "items": [
      { "type": "tips", "helpText": "升级前会自动备份登录态，升级后自动恢复，正常情况下无需重新登录；若升级后 /v1/models 为空，请重新拷贝 loomy-profile。" },
      { "type": "tips", "helpText": "1.1.0 变更：真流式输出（首字延迟大幅下降）、并发限流与超时、客户端断开即中止上游、每请求独立缓冲消除串扰、/v1/models 缓存、结构化访问日志、内存上限 1G→1.5G、新增健康检查。访问日志可在容器日志中查看。" }
    ]
  },
  {
    "stepTitle": "性能与资源",
    "items": [
      {
        "type": "text",
        "field": "loomy_disable_gpu",
        "label": "渲染减负开关",
        "initValue": "0",
        "helpText": "填 0（默认）：保持原行为，空闲时浏览器仍常驻约 25~60% 的单核 CPU，并持续产生缓存写入。填 1：禁用软件 GPU 与光栅化，实测空闲常驻 CPU 可降到 5% 以下、磁盘写入基本归零。若开启后发现 /v1/models 或对话异常，改回 0 重启即可，不影响登录态。",
        "rules": [
          { "required": true, "message": "请输入 0 或 1" },
          { "pattern": "^(0|1)$", "message": "只能填 0（关闭）或 1（开启）" }
        ]
      }
    ]
  }
]
