[
  {
    "stepTitle": "登录态准备（必读）",
    "items": [
      { "type": "tips", "helpText": "本应用靠持久化浏览器会话持有 Loomy 的 HttpOnly 登录 Cookie。飞牛没有图形界面，无法在设备上扫码登录，所以登录态必须先在有界面的电脑上生成，再拷贝到本应用。" },
      { "type": "tips", "helpText": "① 在 Windows / Mac / 带桌面的 Linux 上执行 node login.js，弹出浏览器后扫码或验证码登录，关窗即保存；② 把生成的 loomy-profile 目录内的【全部内容】拷贝到飞牛的 /var/apps/loomyproxy/target/docker/data/loomy-profile/；③ 回到应用中心重启本应用。" },
      { "type": "tips", "helpText": "不放登录态也能装，但 /v1/models 会返回空数组，调用会 401。" }
    ]
  },
  {
    "stepTitle": "访问与验证",
    "items": [
      { "type": "tips", "helpText": "OpenAI 兼容端口：19999。模型列表 GET /v1/models；对话 POST /v1/chat/completions，模型名可用 loomy/<模型ID> 或直接 <模型ID>。" },
      { "type": "tips", "helpText": "验证：curl http://<飞牛IP>:19999/v1/models，返回非空 data 数组即登录态有效。" }
    ]
  },
  {
    "stepTitle": "性能与资源",
    "items": [
      {
        "type": "text",
        "field": "loomy_disable_gpu",
        "label": "渲染减负开关",
        "initValue": "0",
        "helpText": "填 0（默认）：保持原行为，容器内用软件渲染，空闲时浏览器仍常驻约 25~60% 的单核 CPU，并持续产生缓存写入。填 1：禁用软件 GPU 与光栅化，实测空闲常驻 CPU 可降到 5% 以下、磁盘写入基本归零。若开启后发现 /v1/models 或对话异常（页面依赖 WebGL/Canvas 时可能出现），在应用设置里改回 0 重启即可，不影响登录态。",
        "rules": [
          { "required": true, "message": "请输入 0 或 1" },
          { "pattern": "^(0|1)$", "message": "只能填 0（关闭）或 1（开启）" }
        ]
      },
      { "type": "tips", "helpText": "不确定就先填 0，等稳定运行后在应用设置里改成 1 观察一段时间。" }
    ]
  },
  {
    "stepTitle": "注意事项",
    "items": [
      { "type": "tips", "helpText": "⚠️ 登录态会过期：401 或 /v1/models 变空时，需重新在有界面的电脑上登录并再次拷贝 loomy-profile。" },
      { "type": "tips", "helpText": "⚠️ 容器需联网访问 loomy.xunfei.cn；镜像含 Chromium，首次拉取较大；常驻内存实测约 650MB，已设 1.5G 上限。" }
    ]
  }
]
